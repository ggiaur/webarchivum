[pytest]
# Scope collection to tests/ only — this directory also contains real crawl
# output (WACZ/WARC/browser-profile dirs from Docker runs, some root-owned)
# that pytest must never try to walk into.
testpaths = tests
